import Vue from 'vue'
import Vuex from 'vuex'
import * as files from './modules/files'

Vue.use(Vuex)

export default new Vuex.Store({
    modules: {
        files,
    },
    state: {
    },
    mutations: {
    },
    actions: {
    }
})
